apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: platform-api
spec:
  selector:
    matchLabels:
      monitoring: platform-api
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
